import 'package:flutter/material.dart';

class DesignColors {
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFAFAFA);
  static const Color primary = Color(0xFF28D339); // Vibrant Green from screenshot
  static const Color secondary = Color(0xFFE8E8E8);
  static const Color textPrimary = Color(0xFF111111);
  static const Color textSecondary = Color(0xFF888888);
  static const Color accent = Color(0xFF44FF44); // Bright Lime for buttons
}

class DesignSpacing {
  static const double xs = 4.0;
  static const double s = 8.0;
  static const double m = 16.0;
  static const double l = 24.0;
  static const double xl = 32.0;
  static const double xxl = 40.0;
}

class DesignRadius {
  static const double s = 8.0;
  static const double m = 12.0;
  static const double l = 20.0;
  static const double xl = 32.0;
  static const double xxl = 40.0;
}
